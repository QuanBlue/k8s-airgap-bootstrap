---
- name: Stage offline DEB packages into apt cache
  shell: cp /tmp/k8s-packages/*.deb /var/cache/apt/archives/ 2>/dev/null || true
  when: airgap.enabled | bool
  changed_when: false

- name: Install keepalived and HA dependencies via apt (resolves from local cache)
  apt:
    name:
      - keepalived
      - socat
      - conntrack
      - ipset
      - ipvsadm
    state: present
    update_cache: no
  when: airgap.enabled | bool
  environment:
    DEBIAN_FRONTEND: noninteractive

- name: Ensure keepalived config directory exists
  file:
    path: /etc/keepalived
    state: directory
    mode: '0755'

- name: Create keepalived health check script
  copy:
    dest: /etc/keepalived/check_apiserver.sh
    content: |
      #!/bin/sh
      errorExit() {
          echo "*** $*" 1>&2
          exit 1
      }
      curl --silent --max-time 2 --insecure https://localhost:{{ k8s_ha.vip_port }}/ -o /dev/null || errorExit "Error GET https://localhost:{{ k8s_ha.vip_port }}/"
      if ip addr | grep -q {{ k8s_ha.vip_address }}; then
          curl --silent --max-time 2 --insecure https://{{ k8s_ha.vip_address }}:{{ k8s_ha.vip_port }}/ -o /dev/null || errorExit "Error GET https://{{ k8s_ha.vip_address }}:{{ k8s_ha.vip_port }}/"
      fi
    mode: '0755'

- name: Configure Keepalived
  template:
    src: keepalived.conf.j2
    dest: /etc/keepalived/keepalived.conf
    mode: '0644'
  notify: restart keepalived

- name: Enable and start Keepalived
  systemd:
    name: keepalived
    state: started
    enabled: yes
